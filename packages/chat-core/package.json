{
  "name": "@allenlabs/chat-core",
  "version": "0.1.0",
  "description": "Framework-agnostic core for a multiplayer AI chat + agents platform: rooms, @agent mentions, and a pluggable OpenAI-compatible provider. MIT.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "files": ["src"],
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.7",
    "typescript": "5.7.2",
    "vitest": "4.1.7"
  }
}
